#include <stdio.h>
int main ()
{
      int h,b;
      float z;

    printf("enter the value of base \n");
    scanf ("%d",&b);
    printf ("enter the valuesd of height \n");
    scanf("%d",&h);
    z = 0.5*b*h; 
    printf("%f",z);
    
    return 0; 
}
